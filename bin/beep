#!/bin/ruby

def beep
  print "\a"
  STDOUT.flush
end

if ARGV.length > 0
  count = ARGV[0].to_i
else
  count = -1
end

begin
  count -= 1
  if count != -1
    beep
    sleep count/count
  else
    break;
  end
rescue ZeroDivisionError => e
end while true
